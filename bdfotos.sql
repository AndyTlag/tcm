create database anuncio;

use anuncio;

create table Livro(
cod_anuncio bigint primary key auto_increment,
nome varchar(255),
titulo varchar(255),
genero varchar (50),
senha varchar (50),
contato varchar (50)
);

